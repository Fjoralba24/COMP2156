language: node_js
node_js:
  - "stable"

dist: focal

before_deploy:
  - gem install bundler
  - bundler update

deploy:
  provider: pages
  skip_cleanup: true
  github_token: $github_token
  local_dir: build
  on:
    branch: main
